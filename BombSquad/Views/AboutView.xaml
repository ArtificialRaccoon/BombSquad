<Window x:Class="BombSquad.Views.AboutView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About BombSquad" Height="300" Width="300">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="BombSquad" HorizontalContentAlignment="Center" FontSize="36" FontFamily="Impact"/>
        <Label Grid.Row="1" Content="by Bernard Walker, 2017" HorizontalContentAlignment="Center" FontSize="10"/>
        <ListBox Grid.Row="3" ScrollViewer.VerticalScrollBarVisibility="Visible">
            
        </ListBox>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="OK" Width="75"/>
        </Grid>
    </Grid>
</Window>
